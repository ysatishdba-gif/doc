import json
d = json.load(open("temporal_name_to_cui.json"))
positional = [n for n, v in d.items() if any(e.get("formula") is None for e in v)]
print(len(positional), "non-duration concepts:", positional[:40])
