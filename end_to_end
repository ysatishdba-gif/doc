import requests, extract_concepts as E
text = E_recon = __import__("pickle").load(open(E.INPUT_PATH,"rb"))[27]["reconstructed"]["blobs"][5]
r = requests.post(E.API_URL, json=E.build_payload(text), headers=E.get_token(), timeout=120)
print(r.status_code); print(r.text[:1500])
