import pickle

cache = pickle.load(open("doc_side_cache.pkl", "rb"))
# optional extras, if you uploaded them too:
try:    topics = pickle.load(open("topics.pkl", "rb"))
except: topics = {}
try:    recon  = pickle.load(open("reconstructed_obj.pk", "rb"))
except: recon  = None

# ---- 1) list docs in the cache ----
for k, rec in cache.items():
    print(k, rec.get("doc_id"), "| blobs:", len(rec.get("blobs", [])),
          "| topics:", len(rec.get("topic_ids", [])))
