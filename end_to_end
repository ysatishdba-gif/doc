import requests, extract_concepts as E, pickle, time
text = pickle.load(open(E.INPUT_PATH,"rb"))[27]["reconstructed"]["blobs"][5]
for i in range(5):
    r = requests.post(E.API_URL, json=E.build_payload(text), headers=E.get_token(), timeout=120)
    print(i, r.status_code)
    time.sleep(10)
