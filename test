import pickle

cache = pickle.load(open("umls_cache.pkl", "rb"))

SNOMED_CUIS = cache["cuis"]      # set of ~374,538 SNOMED CUIs
TAGS        = cache["tags"]       # dict cui -> STY codes
LABELS      = cache["labels"]     # dict cui -> preferred name (optional but useful)

print(f"SNOMED_CUIS: {len(SNOMED_CUIS):,}")
print(f"TAGS: {len(TAGS):,}")
print(f"LABELS: {len(LABELS):,}")
