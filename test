# Find candidates
candidates = [n for n in dir() if 'rel' in n.lower() or 'mr' in n.lower()]
print("Possible names:", candidates)

# Or if it's inside umls_cache:
if 'umls_cache' in dir():
    print("umls_cache keys:", list(umls_cache.keys()) if hasattr(umls_cache, 'keys') else type(umls_cache))
