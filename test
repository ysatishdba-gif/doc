import json
d = json.load(open("temporal_name_to_cui.json"))
positional = sorted(k for k in d if not any(c.isdigit() for c in k))  # non-duration names
print(len(positional))
for k in positional[:150]:
    print(k)
