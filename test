"""
inspect_isa_subgroups.py
------------------------
Show IS-A subgroups for sample CUIs.

For each CUI, displays its main topic members partitioned into IS-A
subgroups (members sharing rollup ancestors).
"""

import pickle

PKL_PATH = "topic_with_isa_subgroups.pkl"

CUIS = [
    "C0032285",  # Pneumonia
    "C0011849",  # Diabetes mellitus
    "C0021368",  # Inflammation
    "C0024109",  # Lung
    "C2142181",  # Pain of left knee joint
    "C0559470",  # Allergy to peanut
    "C0013182",  # Allergy to drug
    "C0002792",  # Anaphylaxis
]


with open(PKL_PATH, "rb") as f:
    p = pickle.load(f)

topic_to_cuis     = p["topic_to_cuis"]
isa_subgroups     = p["isa_subgroups"]
ungrouped_members = p["ungrouped_members"]
labels            = p.get("labels", {})


def lbl(c):
    return labels.get(c, "?")


for cui in CUIS:
    print(f"\n{'='*90}")
    print(f"  {cui}  '{lbl(cui)}'")
    print(f"{'='*90}")

    members = topic_to_cuis.get(cui, frozenset())
    print(f"  Main topic size: {len(members)}")

    subgroups = isa_subgroups.get(cui, {})
    ungrouped = ungrouped_members.get(cui, frozenset())

    print(f"  IS-A subgroups : {len(subgroups)}")
    print(f"  Ungrouped      : {len(ungrouped)} members\n")

    # Show subgroups sorted by size (larger first)
    sorted_groups = sorted(subgroups.items(),
                           key=lambda kv: -len(kv[1]))
    for anc, mems in sorted_groups:
        marker = " <-- SELF anchor" if anc == cui else ""
        print(f"  SUBGROUP via ancestor: {anc}  '{lbl(anc)}'{marker}")
        for m in sorted(mems):
            self_mark = " (self)" if m == cui else ""
            print(f"    {m}  '{lbl(m)}'{self_mark}")
        print()

    if ungrouped:
        print(f"  UNGROUPED (no shared IS-A ancestor with other members):")
        for m in sorted(ungrouped):
            self_mark = " (self)" if m == cui else ""
            print(f"    {m}  '{lbl(m)}'{self_mark}")

print("\nDONE")
