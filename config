target_node = 'C0559470'
root = 'SNOMEDCT_US.isa'
all_ancestor_paths = []
ancestors = []
for node in netw.successors(target_node):
    print(node)
    if nx.has_path(netw, root, node): 
        ancestors.extend(list(nx.ancestors(netw, node)))
        path = list(nx.shortest_path(netw, source=root, target=node))
        all_ancestor_paths.append(path)
ancestors = list(set([ele.split('-')[0] for ele in ancestors if '-' in ele]))
