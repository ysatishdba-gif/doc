cui = "C0032285"   # Pneumonia

# 1. is the base CUI a node, and does it have attributes?
if cui in netw:
    print("base node attrs:", netw.nodes[cui])
else:
    print("base CUI is not a direct node")

# 2. check its suffixed nodes' attributes
print("\nsuffixed nodes:")
for s in list(netw.successors(cui))[:5]:
    print(f"  {s}: {netw.nodes.get(s, {})}")

# 3. also peek at a few random nodes to see the general attribute shape
print("\nrandom nodes:")
import itertools
for n in itertools.islice(netw.nodes(data=True), 5):
    print(" ", n)
