import time, random
sample = random.sample(list(TARGET), 200)
t0 = time.time()
for cui in sample:
    desc = set()
    for s in cui_to_suffix[cui]:
        desc |= nx.descendants(H, s)
    _ = len({d.split('-')[0] for d in desc if '-' in d})
dt = time.time() - t0
print(f"200 CUIs took {dt:.1f}s  ->  est. for {len(TARGET):,}: "
      f"{dt/200*len(TARGET)/60:.1f} min")
