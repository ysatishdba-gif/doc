if __name__ == "__main__":
    import os
    if os.path.exists(OUTPUT_PATH):
        print(f"{OUTPUT_PATH} already exists — skipping build. "
              f"Delete it to force a rebuild.")
    else:
        main()
