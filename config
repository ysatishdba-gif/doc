import pickle
DOC_INDEX = 0
CHUNK_NO  = 7      # which chunk to show

RECON = pickle.load(open("reconstructed_obj.pk", "rb"))
blobs = [b for b in (RECON[DOC_INDEX]["reconstructed"].get("blobs") or []) if b and b.strip()]
print(f"[CHUNK {CHUNK_NO}] of {len(blobs)}:\n")
print(blobs[CHUNK_NO])
